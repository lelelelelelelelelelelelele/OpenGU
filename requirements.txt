# OpenGU / GULib runtime dependencies
#
# torch is NOT pinned here because it must be installed against a specific CUDA
# version via the PyTorch index URL. See `GULib-master/experiments/configs/README.md`
# for the server install sequence. Local known-good combo (2026-05-04):
#   Python 3.8.20 / torch 2.2.1+cu121 / torch_geometric 2.6.1
#
# For Python 3.10 servers (recommended): bump numpy as below; everything else
# unchanged. torch_scatter / torch_sparse must match torch+CUDA version (use the
# wheel index https://data.pyg.org/whl/torch-<ver>+<cu>.html).
#
# Audited 2026-05-04: experiments/run.py + Phase A.5 hop-decay + predictions.npz
# cache add zero new third-party deps (only stdlib + already-listed packages).

numpy>=1.24,<2.0          # was numpy==1.21.6 (Py 3.10 needs >= 1.22; 2.0 has breaking changes)
torch_scatter==2.1.2
torch_sparse==0.6.18
torch_geometric==2.6.1
scipy==1.10.1
matplotlib==3.7.5
scikit-learn==1.3.2
ogb==1.3.6
pyyaml==6.0.2
deeprobust==0.2.11
seaborn==0.13.2
munkres==1.1.4
cvxpy==1.5.2
# pymetis==2023.1.1
ipdb==0.13.13
tqdm==4.67.1

# pip install memory-profiler
