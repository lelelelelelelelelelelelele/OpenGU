# 自动汇报规则

1. 汇报文件固定为 `results/_journal/auto_report.md`，仅追加，不覆盖历史记录。  
2. 每个任务都记录：脚本、数据集、模型、方法、比例、日志路径、状态、关键指标。  
3. `SKIP` 只在命中“严格 OK 日志”时允许；否则必须执行并写入新日志。  
4. 错误必须记录 `error_type` 与摘要信息，便于二次排查。  
5. 断点续跑判定只依赖日志文件内容，不依赖额外指标台账。  
