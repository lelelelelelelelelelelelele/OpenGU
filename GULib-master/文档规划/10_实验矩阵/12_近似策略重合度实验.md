---
title: 近似策略重合度与有效性实验
created: 2026-07-09
updated: 2026-07-24
type: supplementary-experiment
status: historical-mechanism-evidence-retained-new-a7-pre-registered
tags: [experiment, approximation, im, if, tracin, gif, supplementary]
paper_target: Appendix A.6/A.7
---

# 近似策略重合度与有效性实验

这是 supplementary experiment 中相对独立的一条证据线：

> IM / IF / TracIn 这些便宜近似，是否真的对准了它们声称要测的信号？对准 reference 的 selector，又是否一定造成最大的实际删除伤害？

这页是 SUP 总入口。公式和人话解释见 [[20_研究框架/21_TracIn变体与GIF关系]]；现代 IM 的正式 1%/5% 计划见 [[22_IM成熟算法可用性与Degree超越实验计划]]；论文证据合同见 [Appendix A.7](../../report/paper/outline/A7_approximation_validity.md)。历史 B/C 与 17-output 结果继续由原报告留档，不再充当新 A.7 或 GU 的预算依据。

---

## 0. 当前判定

| Sprint                   | 问题                                      | 状态   | 已有结论                                               |
| ------------------------ | --------------------------------------- | ---- | -------------------------------------------------- |
| S1 · topology overlap    | 历史 IM 是否只是 degree 的昂贵版                  | 已完成  | 5% set overlap 显示二者不同，但尚未证明 IM 优于 degree          |
| S1b · modern IM validity | 成熟 IM 是否在真实 GU 候选域内又快、可审计且超过 degree       | 已预注册 | canonical 1%/5% selection、spread、时间矩阵尚未运行             |
| S2 · target taxonomy     | 梯度大、参数移动大、目标损失大是否一回事                    | 已完成  | A/B 在当前矩阵里相似，但 B/C 基本是不同排序                         |
| S3 · scalable proxy      | 能否便宜近似 B 和 full GIF                     | 已完成  | B-Hutchinson 和 `p_graph` 分别对准各自 reference          |
| S4 · production / attack | proper TracIn 能否进入 Hybrid 与真实 GU attack | 部分完成 | Score → Selection gate 已过；Hybrid、GU canary、C.6 仍待跑 |

因此，这部分不是“还没成果”。历史 S1 与 S2–S3 已经是可写入 SUP 的有限范围机制证据；S1b 与 S4 决定能否进一步宣称现代 IM 超过 degree，以及这种优势是否进入具体 approximate-GU attack。

---

## 1. 先把三条轴分开

以前把 IF / GIF / TracIn 混成一个桶，是因为同时混了三个问题。

### 1.1 目标轴：想选“对谁影响大”的点

| 层                      | 代表分数                                           | 问的问题               |
| ---------------------- | ---------------------------------------------- | ------------------ |
| A · training magnitude | $\lVert g_v \rVert$                            | 这个点自己的训练梯度大不大      |
| B · parameter change   | $\lVert H^{-1}g_v \rVert$                      | 删掉它后参数预计移动多远       |
| C · target loss        | $\langle \mathrm{source}_v, H^{-1}g_E \rangle$ | 这次参数变化是否会伤害目标集 $E$ |

### 1.2 source 轴：“删一个图节点”包含多少图变化

| Source | 包含什么 | 不包含什么 |
|---|---|---|
| point `g_v` | 候选节点自己的 loss gradient | 邻居和删边后表示变化 |
| simple `grad1_v` | 原图上候选点与受影响邻域 | 真正删图后的邻居梯度变化 |
| full graph `q_v=grad1_v-grad2_v` | 原图与删后图的局部梯度差 | 逐候选从头重训（本轮不做） |

### 1.3 时间轴：看最终模型，还是看训练轨迹

| 时间口径 | 代表分数 | 含义 |
|---|---|---|
| single-final | `<source_v(theta_final), g_E(theta_final)>` | 只看训练结束时的对齐 |
| TracInCP | `sum_c w_c <source_v(theta_c), g_E(theta_c)>` | 沿多个 checkpoint 累计轨迹对齐 |

三条轴不能互相替代。例如，point TracInCP 可能很会造成实际 damage，但它仍然不是 full-graph final GIF 的高保真近似。

---

## 2. IM 近似：没有退化为 degree

历史六数据集 concordance 在记录的 5% candidate budget 上支持：

- IM 与 degree 的 top-k 集合重合低；
- 在更大或更密的图上，两者更分开；
- 因此 IM 保留了独立的 coverage / diffusion 信号。

安全说法是：

> 历史 IM 没有在 set level 退化为 degree；但这不证明它优化的 independent spread 更高，更不证明 attack outcome 更强。

新 A.7 不再做 public split 的固定小 $k$ bridge，而直接使用 canonical OpenGU train candidates：

| 轴 | 新实验合同 |
|---|---|
| Datasets | Cora / CiteSeer / PubMed canonical transductive 80/0/20 |
| Ratios | 1% / 5% of complete train candidates |
| Expected $k$ | Cora 21/108；CiteSeer 26/133；PubMed 157/788 |
| Primary comparison | mature IM vs degree；random 为负控制 |
| Outputs | cold/warm、RSS、保证/证书、score 形态、Jaccard、independent spread + paired CI |
| GU boundary | 只有 spread gate winner 才可另行报备 5% GU canary |

正式 candidate count 与 $k$ 仍必须由 SSH canonical manifest 实测并冻结。Jaccard 只解释选点差异；“超过 degree”必须由 selector-independent spread 证明，GU 攻击优势必须再由 retrain gap 证明。

---

## 3. IF / GIF / TracIn：已经完成的主证据

### 3.1 实验覆盖

[B/C 完整报告](../../reports/bc_target_matrix_REPORT.md) 已完成，并继续作为 A.6 的 selector taxonomy / mechanism evidence。它使用 public fixed split 与小固定预算，因此本页不再复述其矩阵数字，也不把它并入新 A.7 或任何 GU ratio 结论。

### 3.2 17-output GPU cold/warm 实测（2026-07-21）

本轮把 `b_param_lissa` 明确留在历史 numerical-validation 边界，正式 ScoreBundle 固定为其余 **17 个** score/ranking 输出。RTX 4090 上完成 `3 datasets × 3 seeds = 9 cells`：

- `9/9` cells 成功，`153/153` 方法级 cold Selection Artifact 为 `miss_saved`，warm 全部 exact hit，失败数为 `0`；
- ScoreBundle cold total mean/max 为 `6.8038 / 9.1624 s`；warm exact read mean/max 为 `0.3200 / 0.9635 s`；
- 峰值显存最大 allocated/reserved 为 `357.0 / 384.0 MiB`；
- 方法级时间包含逐 Artifact 索引、完整性校验与共享文件系统访问，故 warm hit 的验收含义是“零 producer、身份精确命中”，不是承诺每次 wall-clock 必然短于 cold。

这组数据关闭的是“历史 public-split 17-output ScoreBundle 的 cold/warm、显存与失败状态”证据缺口；它不等同于 canonical OpenGU selector benchmark 或 GU canary，也不自动关闭 Hybrid 或 C.6 transfer gate。逐 cell 与逐方法数字见上述双格式报告。

### 3.2 主结论

| 问题 | 证据 | 结论 |
|---|---:|---|
| A 与 B 在当前矩阵是否很像 | A vs B-LiSSA Spearman `0.962` | 经验上很像，但目标定义不同 |
| B 能否用 shared probes 近似 | B-Hutch vs B-LiSSA `0.968` | 可以；B-Hutchinson 是有效的可扩展近似 |
| B 与 C-full 是否是同一排序 | B-LiSSA vs C-GIF `0.023` | 基本不是同一个目标 |
| 固定 full source 后能否去掉 Hessian | `p_graph` vs `gt_full` `0.984` | 可以；这是当前最强 full-GIF proxy |
| point / simple IF 能否冒充 full GIF | `0.112 / 0.040` | 不能；source 对象不同 |
| 多 checkpoint 是否自动更像 final GIF | graph-3 / graph-6 `0.498 / 0.529` | 不能；trajectory 不等于 final-state reference |

### 3.3 fidelity 与实际 damage 是两个轴

`p_graph` 与 `gt_full` 的实际集合删除效果也很接近，说明它的 reference fidelity 确实传递到了选中集合。但在 `k=7` 时：

- `tracin_cp_point_6` 的平均 validation-loss increase 是 `0.0707`；
- `gt_full` 只有 `0.0118`。

因此：

> “最像 final GIF”与“对有限 top-k 集合造成最大实际伤害”必须分开报告。

---

## 4. 历史 concordance 还能说什么

2026-07-10 之前的三数据集诊断有一个仍然有用的现象：

| 历史比较 | Cora | CiteSeer | PubMed | 现在的正确名称 |
|---|---:|---:|---:|---|
| final eval proxy vs eval-target IF diagnostic | `0.742` | `0.727` | `0.647` | single-final diagnostic，不是标准 TracInCP |
| deployed cross-gradient vs IF diagnostic | `0.113` | `0.142` | `0.098` | Legacy 反例 |

这些数字不再是当前主结论，但它们记录了问题是怎么被发现的。权威历史说明见 [FINDING_tracin_misspecification.md](../../self/related_work/concordance/FINDING_tracin_misspecification.md)。

---

## 5. proper-tracin-v1 已到哪一步

[selection gate 验收报告](../../docs/proper_tracin_v1_selection_gate_ACCEPTANCE_REPORT.md) 已接受第一段正式链路：

```text
pre-compute Recipe
  -> immutable Score Artifact
  -> max-k Selection Artifact
  -> k=14 的有序前缀复用给 k=7 / k=3
```

已通过：

- cold miss → 真实计算；
- warm exact hit → 不再训练或重算 score；
- 同 Recipe 不同 content → fail closed / quarantine；
- Score 与 Selection 的父子引用；
- focused `36 passed`，联合回归 `255 passed`。

尚未通过：

- Hybrid 父 Artifact 与 alpha / normalization gate；
- OpenGU canonical profile 下的单 seed、单 GU canary；
- C.6a surrogate transfer 和 C.6b cross-backbone transfer；
- proper TracIn vs random / degree / IM 的 approximate-GU damage 结论。

所以当前可以说“formal selection identity 已打通”，不能说“production TracIn 或 GU attack 已验收”。

---

## 6. 对旧主矩阵的影响

| 对象 | 处理 |
|---|---|
| 旧 TracIn cells | produced 仍是历史产物，但只能标作 `deployed-cross-gradient-legacy` |
| 旧 Hybrid cells | 复用了 Legacy TracIn 分支，需要 refresh |
| random / degree / pagerank / IM | 不因 TracIn 换版重跑 |
| GraphRevoker | 是另一条 repair 线，不和 TracIn refresh 混为一个问题 |

当前 produced / usable / rerun 数量只从 [WORKPLAN](../../self/dashboard/WORKPLAN.md) 与 [config inventory](../../self/dashboard/config_inventory.html) 读取，本页不复制动态计数。

---

## 7. SUP 和论文可以怎么讲

论文分工固定为：[Appendix A.6](../../report/paper/outline/A6_if_gif_taxonomy.md) 负责 A/B–C–D taxonomy 与组内/组间 concordance，[Appendix A.7](../../report/paper/outline/A7_approximation_validity.md) 负责现代 IM 的时间/保证/score/degree comparison 与 trajectory influence 的 target-direct validity。本页继续作为 production/attack 状态入口，不把 selector fidelity 写成 GU outcome。

可以讲：

1. 历史 IM 保留了不同于 degree 的 coverage 信号；现代 IM 是否超过 degree 仍待 canonical 1%/5% 实验。
2. A/B/C 是不同影响目标；A/B 在当前矩阵里高相关，不代表定义等价。
3. B-Hutchinson 是 B-LiSSA 的可扩展近似；`p_graph` 是当前 full-GIF reference 的最强 Hessian-free proxy。
4. source fidelity 和实际 set-deletion damage 是两个评估轴。
5. proper TracIn 已具备可审计的 Score → Selection 身份，但尚未完成 approximate-GU canary。

不能讲：

- single-final `<g_v,g_E>` 就是标准 TracInCP；
- point IF 或 simple IF 就是 full graph GIF；
- checkpoint 越多就一定更好；
- 旧 TracIn 结果已证明 influence-based attack 弱于 degree；
- selection fidelity 高就必然实际 damage 最大。

---

## 8. 下一个独立 vertical slice

这条 SUP 线的下一步分成两个互不冒充的 gate：

1. **A.7a / CPU selector gate**：先审批并运行 canonical Cora/CiteSeer/PubMed × 1%/5% 的 126 行现代 IM 矩阵；报告时间、RSS、score/certificate、Jaccard 与 independent spread。
2. **A.7a / large gate**：小图 promotion 通过后，才运行 ogbn-arxiv × 1%/5% 的 36 行矩阵。
3. **A.7a / GU canary**：只有 spread winner 才以 5% 进入 Cora/CiteSeer × GIF/GNNDelete，与 degree/random 做 retrain-gap 对照；1% GU 需要二次审批。
4. **A.7b / trajectory gate**：按 70/10/20 validation-target 与真实 5% train-candidate budget 重跑 target-direct 白盒矩阵；旧名义 5%、实际 $k=7$ 的矩阵不再作为结果。
5. selection Jaccard、reference fidelity、independent spread 与 GU retrain gap 分开报告。

正式实验仍未启动。本页更新只完成口径收敛和预注册，不构成运行授权。
